# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files
.env*.local

# course assets — staging holds unpublished candidates; never let a commit sweep it in
/staging/

# review-then-delete holding pen — never commit
/to-delete/

# internal course/production brief — keep local, do not publish to the public repo
/CLAUDE.md

# vercel
.vercel
